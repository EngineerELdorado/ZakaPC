import { SubstringNamePipe } from './substring-name.pipe';

describe('SubstringNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SubstringNamePipe();
    expect(pipe).toBeTruthy();
  });
});
