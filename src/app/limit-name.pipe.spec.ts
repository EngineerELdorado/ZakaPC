import { LimitNamePipe } from './limit-name.pipe';

describe('LimitNamePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitNamePipe();
    expect(pipe).toBeTruthy();
  });
});
